PORT=3001
BIND_HOST=127.0.0.1
DATA_DIR=./data
LOG_LEVEL=info

# Shared secret the PHP frontend must present as X-Api-Key.
# Must be at least 32 chars; the process refuses to start without it.
# Generate with: openssl rand -hex 32
BACKEND_API_KEY=
